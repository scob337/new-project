/** @type {import('tailwindcss').Config} */
const config = {
  theme: {
    extend: {
      fontFamily: {
        coolvetica: ["Coolvetica", "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;
